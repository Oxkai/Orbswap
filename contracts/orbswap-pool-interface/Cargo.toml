[package]
name = "orbswap-pool-interface"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Shared Orbswap pool types + contractclient (no contract impl, so no exported symbols)."

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = "27"
